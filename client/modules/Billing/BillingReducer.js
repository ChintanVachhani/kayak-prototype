// Import Actions
import {  } from './BillingActions';

// Initial State
const initialState = {};

const BillingReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default BillingReducer;
