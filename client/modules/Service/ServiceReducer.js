// Import Actions
import {  } from './ServiceActions';

// Initial State
const initialState = {};

const ServiceReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ServiceReducer;
