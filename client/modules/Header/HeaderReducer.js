// Import Actions
import {  } from './HeaderActions';

// Initial State
const initialState = {};

const HeaderReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default HeaderReducer;
