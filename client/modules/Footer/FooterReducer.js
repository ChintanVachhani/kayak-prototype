// Import Actions
import {  } from './FooterActions';

// Initial State
const initialState = {};

const FooterReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default FooterReducer;
